[build]
  functions = "netlify/functions"

# Rewrite /api/zuper-proxy → Netlify function (same URL pattern as Vercel)
[[redirects]]
  from   = "/api/zuper-proxy"
  to     = "/.netlify/functions/zuper-proxy"
  status = 200
  force  = true
